namespace Entities.Monster.AI.RandomPathing
{
    /*
     * A list of all tags containing "Tile" that a monster with
     *  RandomPathing AI can not walk on
     */
    public enum RPTileIgnoreList
    {
        DeactivatedTile,
        SpikeTile,
        OneWayTile
    }
}